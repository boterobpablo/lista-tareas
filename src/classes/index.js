
// este archivo va a servir para agrupar todas las clases que tengo
// en esta carpeta y exportarlas a traves de un solo archivo.
// este archivo es el que va a tener las referencias a todas las clases
// primero las importo y luego las exporto

import { Todo } from './todo.class';
import { TodoList } from './todo-list.class';

export {
    Todo,
    TodoList
}